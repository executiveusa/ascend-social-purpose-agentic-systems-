# LLM Export Importer Plugin

This lightweight browser-extension skeleton gives nontechnical users a simple place to paste or upload exported ChatGPT/Claude/generic conversation JSON and send it into Mission OS.

It intentionally does **not** scrape private LLM pages. Users should export their own data through each provider's official export flow, then import here.

Configure the API URL in `popup.js`.
